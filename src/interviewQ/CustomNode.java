package interviewQ;
// a class represents a listnode which has a next pointer and a pointer to a random node
public class CustomNode {
	CustomNode next;
	CustomNode random;
	int val;
	public CustomNode(int v) {this.val = v;}
}
